# EXHUMED Environment Configuration
# Copy this file to .env and populate with your actual credentials

# ============================================================================
# UPSTASH REDIS CONFIGURATION (Agent Registry + Session Timeline)
# ============================================================================
# Get these from your Upstash Redis database details page.
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here

# ============================================================================
# UPSTASH VECTOR CONFIGURATION (Discussion Vectors)
# ============================================================================
# Get these from your Upstash Vector index details page.
UPSTASH_VECTOR_REST_URL=https://your-vector-index.upstash.io
UPSTASH_VECTOR_REST_TOKEN=your_upstash_vector_rest_token_here

# ============================================================================
# LLM PROVIDER CONFIGURATION
# ============================================================================
# Configure these for your current provider.
# Example values below use Groq's OpenAI-compatible endpoint.
LLM_API_KEY=your_llm_api_key_here
LLM_MODEL_ID=llama-3.1-8b-instant
LLM_API_BASE_URL=https://api.groq.com/openai/v1

# ============================================================================
# BACKEND CONFIGURATION
# ============================================================================
# FastAPI server settings
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
BACKEND_LOG_LEVEL=info

# ============================================================================
# FRONTEND CONFIGURATION (Next.js)
# ============================================================================
# Public backend origin used by the Next.js frontend.
# Required for production frontend builds.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# When the frontend is deployed on a different origin, set the backend CORS list
# to include that deployed frontend origin.
# Example:
# CORS_ALLOW_ORIGINS=https://your-frontend.example.com

# ============================================================================
# OPTIONAL: MONITORING & LOGGING
# ============================================================================
# Sentry error tracking (optional)
# SENTRY_DSN=https://your_sentry_dsn@sentry.io/project_id

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
