// The app currently has a single route that mounts the full chat workbench.
import { ChatWorkbench } from "@/components/chat-workbench";

export default function Home() {
  return <ChatWorkbench />;
}